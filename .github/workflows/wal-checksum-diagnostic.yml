name: Diagnose Nexus WAL checksum gate

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wal-checksum-diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Run WAL checksum suite with forensic annotation
        shell: bash
        run: |
          set +e
          python tests/test_nexus_wal_checksum_gate.py -v 2>&1 | tee /tmp/wal-checksum-tests.log
          status=${PIPESTATUS[0]}
          set -e
          if [ "$status" -ne 0 ]; then
            python - <<'PY'
          from pathlib import Path
          text = Path('/tmp/wal-checksum-tests.log').read_text(encoding='utf-8', errors='replace')
          tail = text[-14000:]
          escaped = tail.replace('%', '%25').replace('\r', '%0D').replace('\n', '%0A')
          print(f"::error title=Nexus WAL checksum gate failure::{escaped}")
          PY
            exit "$status"
          fi
